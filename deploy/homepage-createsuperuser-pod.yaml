apiVersion: v1
kind: Pod
metadata:
  name: homepage-createsuperuser
  labels: 
    name: homepage-createsuperuser
    version: 0.1.7
spec:
  containers:
    - name: homepage-createsuperuser
      image: gcr.io/ianlewis-org/homepage:0.1.7
      command: 
        - homepage
        - createsuperuser
      env:
        - name: DEBUG
          value: 'False'
        - name: DB_HOST
          value: mysql
        - name: MEDIA_ROOT
          value: /var/www/media
        - name: USE_MEMCACHED
          value: 'False'
        - name: MEMCACHED_HOSTS
          value: memcached:11211
        - name: ENV_DIR
          value: /etc/secrets
      volumeMounts:
        - name: secret-volume
          mountPath: '/etc/secrets'
  volumes:
    - name: secret-volume
      secret:
        secretName: homepage-secret
  restartPolicy: Never
