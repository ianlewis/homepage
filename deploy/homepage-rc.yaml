apiVersion: v1
kind: ReplicationController
metadata:
  name: homepage-0.1.14-1
  labels:
    name: homepage
    version: 0.1.14-1
spec:
  replicas: 2
  selector:
    name: homepage
    version: 0.1.14-1
  template:
    metadata:
      labels:
        name: homepage
        version: 0.1.14-1
    spec:
      containers:
        - name: homepage
          image: asia.gcr.io/ianlewis-org/homepage:0.1.14
          env:
            - name: DEBUG
              value: 'False'
            - name: DB_HOST
              value: mysql
            - name: MEDIA_ROOT
              value: /var/www/media
            - name: USE_MEMCACHED
              value: 'False'
            - name: MEMCACHED_HOSTS
              value: memcached:11211
            - name: HTTPS
              value: "on"
            - name: ENV_DIR
              value: /etc/secrets
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: secret-volume
              mountPath: /etc/secrets
        - name: webfront
          image: asia.gcr.io/ianlewis-org/webfront:1.0.3
          ports:
            - containerPort: 8000
            - containerPort: 8001
          volumeMounts:
            - name: webfront-secrets
              mountPath: /conf
      volumes: 
        - name: secret-volume
          secret:
            secretName: homepage-secret
        - name: webfront-secrets
          secret:
            secretName: webfront-secret  
