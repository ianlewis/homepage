apiVersion: v1
kind: ReplicationController
metadata:
  name: homepage-0.2.1-1
  labels:
    name: homepage
    version: 0.2.1-1
spec:
  selector:
    name: homepage
    version: 0.2.1-1
  template:
    metadata:
      labels:
        name: homepage
        version: 0.2.1-1
    spec:
      containers:
        - name: homepage
          image: asia.gcr.io/ianlewis-org/homepage:0.2.1
          # defines the health checking
          livenessProbe:
            # an http probe
            httpGet:
              path: /_status/healthz
              port: 8080
            # length of time to wait for a pod to initialize
            # after pod startup, before applying health checking
            initialDelaySeconds: 15
            timeoutSeconds: 1
          env:
            - name: DEBUG
              value: 'False'
            - name: DB_HOST
              value: mysql
            - name: MEDIA_ROOT
              value: /var/www/media
            - name: USE_MEMCACHED
              value: 'False'
            - name: MEMCACHED_HOSTS
              value: memcached:11211
            - name: HTTPS
              value: "on"
            - name: ENV_DIR
              value: /etc/secrets
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: secret-volume
              mountPath: /etc/secrets
          resources:
            limits:
              memory: 256Mi
              cpu: 200m
        - name: nginx
          image: asia.gcr.io/ianlewis-org/nginx:1.9.9-2
          ports:
            - containerPort: 8000
            - containerPort: 8001
          volumeMounts:
            - name: webfront-secrets
              mountPath: /secrets
          resources:
            limits:
              memory: 50Mi
              cpu: 25m 
      volumes: 
        - name: secret-volume
          secret:
            secretName: homepage-secret
        - name: webfront-secrets
          secret:
            secretName: webfront-secret  
