apiVersion: batch/v1
kind: Job
metadata:
  name: homepage-migrate
spec:
  template:
    metadata:
      name: hompage-migrate
      labels:
        name: homepage-migrate
        version: "${HOMEPAGE_VERSION}"
    spec:
      containers:
        - name: homepage-migrate
          image: asia.gcr.io/${PROJECT}/homepage:${HOMEPAGE_VERSION}
          command: ["/venv/bin/homepage", "migrate"]
          env:
            - name: DEBUG
              value: 'False'
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: homepage-secret
                  key: secret-key
            - name: DB_HOST
              value: mysql
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: homepage-admin-secret
                  key: db-admin-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: homepage-admin-secret
                  key: db-admin-password
            - name: MEDIA_ROOT
              value: /var/www/media
            # - name: USE_MEMCACHED
            #   value: 'False'
            - name: ENV_DIR
              value: /etc/secrets
          resources:
            limits:
              memory: 100Mi
              cpu: 50m
      restartPolicy: Never
